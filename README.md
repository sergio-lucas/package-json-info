# Strapi plugin package-json-info

This plugin shows the contents of the package.json file of your project in the Setting page

<img src="plugin.png" />


# Installation

1. Install dependency
```sh
npm i strapi-package-json-info
```
2. Rebuild your project
```sh
npx strapi build
```